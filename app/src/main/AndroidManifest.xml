<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.manhunt">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".GlobalPlayerClass"
        android:allowBackup="true"
        android:icon="@drawable/m_icon_colorised"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Manhunt"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ListOfPlayers"
            android:label="@string/title_activity_list_of_players"
            android:theme="@style/Theme.Manhunt.NoActionBar"></activity>
        <activity
            android:name=".CreateGamePopup"
            android:theme="@style/Theme.AppCompat.CreateGamePopup"></activity>
        <activity android:name=".Options"
            android:theme="@style/Theme.Manhunt.NoActionBar"></activity>
        <activity android:name=".Lobby"
            android:theme="@style/Theme.Manhunt.NoActionBar"></activity>
        <activity android:name=".Game"
            android:theme="@style/Theme.Manhunt.NoActionBar"></activity>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Start"
            android:label="@string/app_name"
            android:theme="@style/Theme.Manhunt.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
    </activity>
        <activity
            android:name=".ListofLobbies"
            android:label="Lobbies"
            android:theme="@style/Theme.AppCompat.popupTheme" />
    </application>

</manifest>